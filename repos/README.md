# Attached repositories

This directory is ignored except for this file. `bin/init` populates it from
`workspace.toml`; every resulting checkout has an independent Git history.

Do not add source trees to the workspace repository index.
